大意：一个数列a，求有多少对(l,r)使得删去[l,r]范围内的数字后，a有序
r <= x, 1 <= n, x <= 10^6

题解：
1.若(l,r)满足，则(l,r+1)满足
2.若(l,r)，(l+1, r')满足，则r' >= r
所以枚举l，不断右移r使(l,r)满足并统计答案。
满足条件：
	记m_i为，位于i右侧的小于a_i的最小的数，则r = (l, max {a_i | m_i < l})
注意：
	l的最大值为最小的a_i，i右侧有比a_i小的数，r的最小值为最大的i右侧比a_i小的数