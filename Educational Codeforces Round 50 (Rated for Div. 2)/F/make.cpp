#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#include <time.h>
#define LL long long
#define db double
#define Hzy(o) freopen(o".in", "r", stdin), freopen(o".out", "w", stdout)
using namespace std;
int T = 100000;
LL Rg = 1e18;

int main() {
	freopen("F.in", "w", stdout);
	srand(time(0));
	cout << T << endl;
	for(int i = 1; i <= T; ++i)
		cout << Rg  << endl;
	return 0;
}
